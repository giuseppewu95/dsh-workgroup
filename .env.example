# .env.example

# dsh-workgroup has no required environment variables.
#
# DeepSeek Harness plugin development may need the harness itself available
# on PATH (`dsh --profile web`) and a `DEEPSEEK_API_KEY` only when running
# real-API tests of the harness, not of this plugin.
